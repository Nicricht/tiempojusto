route:
  receiver: staging-ops
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

# Deployment-specific notification credentials/URLs stay outside Git.
# The staging operator must add an approved receiver integration through the
# secret-backed deployment configuration before declaring alert delivery proven.
receivers:
  - name: staging-ops
